specVersion: "v1"
serviceId: "safeschools-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "safeschools-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
